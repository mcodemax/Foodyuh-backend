refactor notes:

fdcapi:

//notes: add validatoin layer to check parameters, so don't even need to make req to fdcapi

split into 3 diff routes,
//even need to expose these, just have frontend call api directly, justification>I made backend first


/*
Summary of routes in plates:
  POST / { plate } =>  { plate }
  DELETE /delete/[plateId] => { deletedPlate }
  GET /  => { user }
  GET /[plateId]  =>  { plate }
  POST /[plateId] {fdcId} =>  { plate }
  DELETE /[plateId] => { plate }
*/